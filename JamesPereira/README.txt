Foi necessário ajustar o Makefile, que especificava arquivo com nome diferente do existente.
Usa módulo register_file e alu que não existe no código.

Pedi um MIPS 32 semelhante ao especificado no livro, 
entregou um de 16 bits possivelmente não autoral.

Lamento, é 0.
